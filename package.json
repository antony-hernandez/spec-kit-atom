{
  "name": "atomic",
  "version": "0.20.0",
  "description": "Asistente de desarrollo spec-driven para el equipo de Atom",
  "bin": {
    "atomic": "./packages/cli/src/install.mjs"
  },
  "type": "module",
  "scripts": {
    "setup-dev": "cp packages/cli/hooks/pre-push .git/hooks/pre-push && chmod +x .git/hooks/pre-push && echo '✓ git hooks instalados'"
  },
  "engines": {
    "node": ">=18"
  }
}
